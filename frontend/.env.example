# 前端环境变量配置
#
# 使用方式：
#   1. 复制本文件为 .env.production：cp .env.example .env.production
#   2. 填写真实值
#   3. 确保 .env.production 已加入 .gitignore
#
# 注意：只有以 VITE_ 开头的变量才会被暴露给前端代码

# ECS 公网 IP（用于作品集页面中的项目跳转链接）
VITE_ECS_IP=YOUR_ECS_IP
